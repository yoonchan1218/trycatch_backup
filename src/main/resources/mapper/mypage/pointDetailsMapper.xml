<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.trycatch.mapper.mypage.PointDetailsMapper">

    <select id="selectAllByMemberId" resultType="PointDetailsDTO">
        select
            id, member_id, point_type, point_amount, created_datetime, updated_datetime
        from tbl_point_details
        where member_id = #{memberId}
        order by created_datetime desc
    </select>

    <insert id="insert">
        insert into tbl_point_details (member_id, point_type, point_amount)
        values (#{memberId}, #{pointType}, #{pointAmount})
    </insert>

</mapper>
