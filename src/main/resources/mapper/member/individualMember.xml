<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.trycatch.mapper.member.IndividualMemberMapper">
    <insert id="insert">
        insert into tbl_individual_member (id, individual_member_birth, individual_member_gender)
        values(#{id}, #{individualMemberBirth}, #{individualMemberGender})
    </insert>

    <select id="selectById" resultType="IndividualMemberDTO">
        select * from view_member_profile
        where id = #{id}
    </select>
</mapper>