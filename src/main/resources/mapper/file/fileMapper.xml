<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.trycatch.mapper.file.FileMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_file(file_path, file_name, file_original_name, file_size, file_content_type)
        values(#{filePath}, #{fileName}, #{fileOriginalName}, #{fileSize}, #{fileContentType})
    </insert>

    <delete id="delete">
        delete from tbl_file
        where id = #{id}
    </delete>

    <select id="selectById">
        select
            id, file_path, file_name, file_original_name, file_size,
            file_content_type, created_datetime, updated_datetime
        from tbl_file
        where id = #{id}
    </select>
</mapper>
