<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.trycatch.mapper.corporate.CorpWelfareRelMapper">
    <insert id="insertAll">
        INSERT INTO tbl_corp_welfare_rel (corp_id, welfare_code, welfare_name)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.corpId}, #{item.welfareCode}, #{item.welfareName})
        </foreach>
    </insert>

    <delete id="deleteByCorpId">
        DELETE FROM tbl_corp_welfare_rel
        WHERE corp_id = #{corpId}
    </delete>

    <select id="selectByCorpId" resultType="CorpWelfareRelVO">
        SELECT id, corp_id, welfare_code, welfare_name
        FROM tbl_corp_welfare_rel
        WHERE corp_id = #{corpId}
        ORDER BY id
    </select>
</mapper>
