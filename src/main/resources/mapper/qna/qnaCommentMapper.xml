<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.trycatch.mapper.qna.QnaCommentMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_qna_comment(qna_id, member_id, qna_comment_parent, qna_comment_content)
        values(#{qnaId}, #{memberId}, #{qnaCommentParent}, #{qnaCommentContent})
    </insert>

    <select id="selectByQnaId" resultType="QnaCommentDTO">
        select *
        from view_qna_comment
        where qna_id = #{qnaId}
        order by created_datetime asc
    </select>

    <delete id="delete">
        delete from tbl_qna_comment where id = #{id} and member_id = #{memberId}
    </delete>

    <update id="update">
        update tbl_qna_comment
        set qna_comment_content = #{qnaCommentContent}
        where id = #{id} and member_id = #{memberId}
    </update>

    <select id="selectMemberIdById" resultType="Long">
        select member_id from tbl_qna_comment where id = #{id}
    </select>

    <select id="existsByQnaIdAndMemberId" resultType="boolean">
        select count(*) > 0 from tbl_qna_comment
        where qna_id = #{qnaId} and member_id = #{memberId} and qna_comment_parent is null
    </select>

</mapper>