<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.trycatch.mapper.qna.QnaCommentLikesMapper">
    <insert id="insert">
        insert into tbl_qna_comment_likes(id, fk_member_id)
        values(#{id}, #{fkMemberId})
    </insert>

    <delete id="delete">
        delete from tbl_qna_comment_likes where id = #{id}
    </delete>

    <select id="countByCommentId" resultType="int">
        select count(*) from tbl_qna_comment_likes where id = #{commentId}
    </select>

</mapper>