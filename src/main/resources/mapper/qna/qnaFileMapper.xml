<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.trycatch.mapper.qna.QnaFileMapper">
    <insert id="insert">
        insert into tbl_qna_file(id, qna_id)
        values(#{id}, #{qnaId})
    </insert>

    <delete id="deleteById">
        delete from tbl_qna_file where id = #{fileId} and qna_id = #{qnaId}
    </delete>
</mapper>
